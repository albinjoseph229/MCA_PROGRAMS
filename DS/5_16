#include <stdio.h>
int ar[100];

int main()
{
    int n,i;
   printf("enter the no of elements: ");
   scanf("%d",&n);
   for(i=0;i<n;i++)
   {
       printf("enter the elemnt at %d: ", ar[i]);
       scanf("%d",&ar[i]);
   }
   for(i=0;i<n;i++)
   {
       printf("%d",ar[i]);
   }
   return 0;
}